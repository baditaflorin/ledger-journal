# Contributing

1. Run `make install-hooks` after cloning.
2. Use Conventional Commits.
3. Run `make lint`, `make test`, and `make smoke` before pushing.
4. Never commit real journal data, passphrases, private keys, `.env` files, or generated proof bundles that contain private text.
